# Codex App Server schema provenance

Generated with:

```text
codex-cli 0.144.5
codex app-server generate-json-schema --out contracts/codex-app-server
```

Regenerate this bundle when the shipped Codex CLI version changes. The generated schema is the protocol source for adapters; handwritten transport types must stay narrow.

